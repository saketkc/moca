.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install moca

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv moca
    $ pip install moca
